{
  "name": "avata-trading",
  "version": "1.0.0",
  "description": "AVATA Trading Platform - E-commerce with Backend and Frontend",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "npm ci --prefix backend --omit=dev || true",
    "start": "node backend/server.js",
    "dev": "cd backend && nodemon server.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
